apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mock-pvc
  namespace: mock
spec:
  storageClassName: mock-sc
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
