apiVersion: v1
kind: Pod
metadata:
  labels:
    run: crasher
  name: crasher
spec:
  containers:
  - image: busybox
    name: crasher
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
