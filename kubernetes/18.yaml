apiVersion: v1
kind: Service
metadata:
  labels:
    app: nginx
  name: web-nodeport
spec:
  ports:
  - port: 80
    nodePort: 30080
    protocol: TCP
    targetPort: 80
  selector:
    app: nginx
  type: NodePort
status:
  loadBalancer: {}
