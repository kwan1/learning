apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-demo-ing
  namespace: exam
spec:
  rules:
  - host: demo.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ingress-demo-svc
            port:
              number: 80
