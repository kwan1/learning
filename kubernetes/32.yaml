apiVersion: v1
kind: Pod
metadata:
  name: secure-pod
  namespace: default
spec:
  containers:
  - name: nginx
    image: nginx:alpine
    securityContext:
      runAsUser: 1000
      readOnlyRootFilesystem: true
      capabilities:
        drop:
          - ALL
        add:
          - NET_BIND_SERVICE

