apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-pvc
  namespace: logging
spec:
  storageClassName: logs
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
