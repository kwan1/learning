apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kustomize-json

resources:
  - ../../base

patches:
  - target:
      kind: Deployment
      name: web
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 4

