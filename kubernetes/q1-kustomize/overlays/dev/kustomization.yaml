apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kustomize-dev
namePrefix: dev-

labels:
  - pairs:
      environment: dev

resources:
  - ../../base

replicas:
  - name: web
    count: 3
